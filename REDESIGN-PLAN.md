# План редизайна всех экранов otfk

Рабочий файл (не коммитить). Конвейер на каждый пункт: скрин с https://just-test.shop → ChatGPT-макет → имплементация «по мотивам» → тесты/сборка → деплой в `feature/**` (детали и грабли — в памяти `chatgpt-design-pipeline`).

Статусы: `[x]` готово · `[ ]` в очереди · `[~]` в работе.

## 0. Глобальный каркас (проверяется на каждой странице, но правится один раз)

- [x] **Главная** — `/` — `home.blade.php` + `components/home/hero-slider.blade.php` (коммит `f672178`)
- [x] **Хедер: топбар, меню, мобильное меню, поиск** — `components/layouts/app.blade.php` (меню DB-driven, 12 пунктов — проверять на реальных данных)
- [x] **Футер** — там же, `components/layouts/app.blade.php`
- [x] **Хлебные крошки** — `components/breadcrumbs.blade.php` (ChatGPT-макет новостей сохранил текущий вид: Головна › Новини в hero — оставлено как есть)
- [x] **Общие компоненты**: `page-hero`, `news-card`, `staff-card`, `empty-state`, `lead-excerpt`, `heritage-frame`, `prose/article` — единый стиль зафиксирован по ходу основного прохода (светлая шапка-карточка вместо navy `page-hero` на внутренних страницах, `accent-rule`, `card`/`card-interactive`, украинская пагинация)
  - `breadcrumbs` получил проп `tone` (`dark` по умолчанию, `light` — для страниц со светлой шапкой; впервые задействован на детальной новости)
  - `department-card` — новый компонент (иконка типа подразделения, название, уривок описания, счётчик сотрудников); используется списком структуры и блоком «Інші підрозділи» на детальной
  - `specialty-card` — новый компонент (navy-шапка с кодом + иконка + мета-строки); используется списком и блоком «Інші спеціальності» на детальной, проп `show-program` выключает опис и ОП-программу

## 1. Новости и медиа

- [x] **Список новостей** — `/novyny` — `news/index.blade.php` (featured-карточка на 1-й странице без фильтров; бейдж категории на фото; подзаголовок в hero; украинская пагинация `vendor/pagination/tailwind.blade.php` — общая, задействована и на `/video`)
- [x] **Новость (детальная)** — `/novyny/{slug}` — `news/show.blade.php` (светлая шапка-карточка с контуром здания вместо navy-героя, время чтения, сайдбар с миниатюрами + «Корисно для абітурієнта» из меню, «предыдущая/следующая новость», финальный CTA «Стати студентом»; тесты — `NewsArticleLayoutTest`)
- [x] **Видео** — `/video` — `videos/index.blade.php` (светлая шапка-карточка с контуром плеера и счётчиком роликов; featured-видео на 1-й странице; сетка 3–4 в ряд; плеер открывается в лайтбоксе на youtube-nocookie вместо ухода на YouTube; навигационный CTA-блок соцсетей — новый ключ настроек `social_youtube` (миграция `2026_08_28_130000`, пункт в «Що заповнити»); тесты — `VideoPageTest`)
  - Длительность роликов и категории видео в макете ChatGPT есть, но в БД их нет — не выдумывали (нужен YouTube API / новое поле)
- [x] **Галереи (список)** — `/halereya` — `galleries/index.blade.php` (светлая шапка-карточка с контуром фото и склоняемым счётчиком «N альбом/альбоми/альбомів»; рекомендованный альбом большим блоком на 1-й странице; сетка 3–4 в ряд; пагинация по 12 — контроллер переведён с `get()` на `paginate(12)`; архивные альбомы помечены золотым бейджем «Архів», кольцом и сепией; тесты — `GalleryPageTest`)
- [x] **Галерея (детальная, лайтбокс)** — `/halereya/{slug}` — `galleries/show.blade.php` (светлая шапка с CTA «Почати перегляд»; мозаичная сетка `grid-flow-dense` — каждое 5-е фото плиткой 2×2, подписи в градиенте, лупа при наведении; лайтбокс с перелистыванием стрелками/клавиатурой, счётчиком «N / M» и панелью подписи; блок «Інші альбоми» + возврат к галерее; `.photo-archive` остаётся на секции всегда — этого требует `LampaTwoTest`)
  - Даты-диапазоны архивных альбомов («1980–1989») и длительности в макете ChatGPT есть, но в БД таких полей нет — не выдумывали

## 2. Абитуриенту / интерактив

- [x] **Заявка абитуриента (форма)** — `/zayavka` — `applicants/create.blade.php` (светлая шапка-карточка с контуром выпускной шапочки; двухколонковый layout `lg:grid-cols-3` — форма на 2 колонки, сайдбар на 1: «Що буде далі» (3 шага), «Контакти приймальної комісії» (DB-driven из `settings`, блок скрыт, если все ключи пусты), золотая карточка-CTA на квиз; крупные поля `px-4 py-3 text-base` + `inputmode=tel/email` и `autocomplete`; счётчик символов 0/2000; сводный баннер ошибок валидации над формой; полоса из 4 преимуществ внизу; тесты — `ApplicantFormLayoutTest`)
- [x] **Специальности (список)** — `/spetsialnosti` — `specialties/index.blade.php` (светлая шапка-карточка со счётчиком «N спеціальність/спеціальності/спеціальностей»; обложек в БД нет — карточки держатся на navy-шапке с огромным кодом специальности, иконке направления и SVG-сетке; сетка 2×2; мета-строки степень/форма/срок + название ОП-программы; финальный CTA «Подати заявку / Пройти квіз»; тесты — `SpecialtyPageTest`)
- [x] **Специальность (детальная)** — `/spetsialnosti/{slug}` — `specialties/show.blade.php` (navy-шапка: слева бейдж кода, заголовок, короткий опис и чипы, справа гигантский код `text-[9rem] text-white/10` + золотая иконка направления — скрыты на `<lg`; золотая карточка «Про спеціальність»; блок ОП-программ; золотая полоса «Потрібна консультація?» → `/kontakty`; липкий сайдбар «Деталі навчання» с CTA «Подати заявку» (предзаполнение через `?specialty_id=`) и «Пройти квіз на вибір спеціальності»; «Інші спеціальності» теми же карточками внизу; разметка `Course` сохранена)
  - Декоративная иконка направления — акцессор `Specialty::icon_name`: точный код → галузь знаний (первые 2 цифры) → `academic-cap`; данных об иконках в БД нет, это оформление
  - Названия ОП-программ в БД уже содержат слова «Освітньо-професійна програма» — лейблы у них не дублируем (ни в карточке, ни в таблице деталей)
  - Макет детальной ChatGPT отдал с задержкой ~15 мин (вкладка требует ручного обновления) — реализация выровнена по нему
- [x] **FAQ** — `/faq` — `faq/index.blade.php` (светлая шапка-карточка с контуром облачков диалога и золотым бейджем-счётчиком «N питання/питань»; живой клиентский поиск по вопросам и ответам на Alpine — фильтрация без запросов на сервер, строка «Знайдено N» и состояние «нічого не знайдено» с кнопкой сброса; аккордеон пронумерованными карточками (`aria-expanded`/`aria-controls`, открытая — золотое кольцо), две колонки только от 4 вопросов, иначе одна; липкий сайдбар «Не знайшли відповідь?» с DB-контактами приймальної комісії (`contact_*`, `work_hours` — блок скрыт, если пусто) + соцсети из `social_*`; финальная полоса «Є питання щодо вступу?»; разметка `FAQPage` сохранена; тесты — `FaqPageTest`)
  - Макет ChatGPT рисовал 12 вопросов, в БД их 3 — при коротком списке сайдбар выше контента, поэтому две колонки включаются только от 4 вопросов
- [x] **Квиз** — `/kviz` — `quiz/index.blade.php` (светлая шапка-карточка с контуром пазла и тремя чипами «N питань · близько хвилини · без реєстрації»; интро в две колонки: «Як це працює» из 3 шагов + крупная кнопка старта, сайдбар «Спеціальності в тесті» — реальные специальности из БД с иконками направления; экран вопроса — сегментированный прогресс (полоса + сегмент на каждый вопрос), «Назад», варианты 2×2 крупными кнопками (мобильный — 295×80), выбор клавишами 1–4; **вопросы и варианты рендерит сервер** (`data-step`/`data-opt`) — видны без JS и в тестах, Alpine только считает баллы; результат — navy-карточка победителя с иконкой направления и бейджем «Збіг N%», ниже разбор «Як розподілилися твої відповіді» полосами по всем специальностям с ненулевым баллом, CTA «Залишити заявку» с `?specialty_id=` и «Про спеціальність», «Пройти ще раз», финальная полоса с контактами приймальної комісії; тесты — `QuizPageLayoutTest`)
  - Макет ChatGPT не делали: расширение Claude in Chrome не подключено в этой сессии — страница собрана по устоявшемуся дизайн-языку остальных внутренних страниц
  - Грабли: `truncate` (nowrap) в сайдбаре раздувал min-content grid-колонки и давал горизонтальный скролл на 375px — заменено на перенос строки
- [x] **События** — `/podiyi` — `events/index.blade.php` (светлая шапка-карточка с контуром календаря и чипами «N подій попереду · Найближча — 31 серпня · N подій в архіві»; ближайшее событие — большим блоком: navy-панель с числом, месяцем в родительном падеже и днём недели + SVG-сетка, справа золотой бейдж «Найближча подія» и отсчёт «Сьогодні/Завтра/Через N днів»; остальные — сеткой 2–4 в ряд компонентом `event-card`; кнопки календаря вынесены в общий `event-calendar-links` (Google + «Завантажити .ics»); «Минулі події» — компактные строки со светлым блоком даты и годом справа; пустое состояние переписано в широкую карточку с CTA «Перейти до новин» и «Запитати в коледжу»; финальная полоса «Будьте в курсі всіх подій коледжу»; контроллер отдаёт `pastCount` для счётчика архива; разметка `Event` сохранена; тесты — `EventsPageLayoutTest`)
  - Событий на проде нет — страница проверялась на локальных демо-событиях; на пустой БД отрабатывает состояние-заглушка
  - Категорий/типов событий и обложек в БД нет — иконки-«типы» и фото из макета ChatGPT не выдумывали
  - Мобильная версия выведена из десктопной: одна колонка, на 375px горизонтального скролла нет
- [x] **Расписание звонков** — `/rozklad-dzvinkiv` — `bells/index.blade.php` (светлая шапка-карточка с контуром часов и чипами «N пар · Дві зміни · Пара — 70 хв»; **две смены** — по карточке на смену рядом в `lg:grid-cols-2`, в каждой navy-шапка со временем работы смены и лента пар с перерывами между ними; текущая пара подсвечивается золотом, бейдж «зараз · N хв» и полоса прохождения; смены накладываются 13:00–14:00, поэтому подсвечиваются обе пары сразу — `bellState()` теперь возвращает массив ключей «смена:номер»; реальные времена со стенда колледжа — миграция `2026_08_28_150000` (колонка `shift` + настройка `bells_second_shift`); вторая смена выключается кнопкой «Вимкнути другу зміну» в админке — тогда одна центрированная карточка и чип «Одна зміна»; тесты — `BellScheduleLayoutTest`)
  - Времена и обе смены редактируются в Filament (`Розклад дзвінків`): поля «Зміна» + «Номер пари»; перерывы по-прежнему считаются из промежутков

## 3. О колледже / структура

- [x] **Структура (список подразделений)** — `/struktura` — `structure/index.blade.php` (светлая шапка-карточка с контуром здания и чипами «N підрозділів · N співробітників»; под шапкой — полоса пилюль-якорей по трём группам с числом подразделений в каждой (`#viddilennya`, `#tsyklova-komisiya`, `#kafedra`, тач-мишень 44px на мобильном); заголовки групп во множественном числе (`Department::TYPES` хранит единственное — оно нужно бейджем на детальной), navy-квадрат с иконкой типа + бейдж-счётчик; карточки 2×2 → 4 в ряд общим компонентом `department-card` (иконка типа, название, уривок описания из импортированного HTML, число сотрудников, «Детальніше»); финальная полоса «Не знайшли потрібний підрозділ?» → `/administratsiya` и `/kontakty`; тесты — `StructurePageTest`)
  - Обложек и описаний групп в БД нет — иконка на группу выводится из типа (их ровно три в `Department::TYPES`), ничего не выдумывали
- [x] **Подразделение (детальное)** — `/struktura/{slug}` — `structure/show.blade.php` (navy-герой заменён светлой шапкой-карточкой: бейдж типа, заголовок, чип «N співробітників», контур иконки типа справа; двухколоночный `lg:grid-cols-3` — импортированное описание в `x-prose.article` (`prose-site`, без drop cap: колонка 65ch, фото больше не растягиваются на 1270px — страница ужалась с 22 300px до 19 300px), липкий сайдбар «Коротко про підрозділ» (тип, состав) + «Швидкі посилання» (структура, администрация, специальности, контакты); «Склад підрозділу» на светлой подложке сеткой 2–4 в ряд прежним `staff-card`; блок «Інші підрозділи» — соседи той же группы (контроллер отдаёт `$others`, до 4) + «До структури»; тесты — `StructurePageTest`)
  - Из макета ChatGPT не реализованы плитки «4 освітні програми», список документов и контакты подразделения — таких полей и связей в БД нет
  - Заголовков в импортированном HTML нет (только `p`/`ul`/`img`/`a`), поэтому «Навігація по сторінці» из макета не собирается — не делали
- [x] **Администрация** — `/administratsiya` — `staff/administration.blade.php` (светлая шапка-карточка с контуром группы людей и чипами «N керівників · Приймальня: телефон · графік роботи» — телефон в чипе кликабельный; **директор отдельным блоком**: крупная монограма, бейдж «Керівник коледжу», посада золотым, кнопки `tel:` + «Профіль керівника», справа текст из `bio` и ссылка на подразделение; остальные — группами «Заступники директора» и «Керівники відділень та служб», роль выводится из `position` акцессором `Staff::administration_role` («заступник» проверяется раньше «директора» — в посаде заступника тоже есть слово «директор»); карточки — новый компонент `leader-card` (монограма, ПІБ-ссылка на профиль, посада, ступінь, `tel:`/`mailto:`, чип подразделения со ссылкой на `/struktura/{slug}`), сетка 2→3→4 в ряд; блок «Контакти приймальні» 4 колонками из `settings` (скрыт, если все ключи пусты); финальная navy-полоса «Потрібна консультація щодо вступу?» → `/zayavka` и `/kontakty`; тесты — `AdministrationPageTest`)
  - Фото сотрудников в БД нет ни у кого — карточки держатся на монограмах-инициалах; в макете ChatGPT были фото — не выдумывали
  - Группировка — единственная эвристика по тексту посады: если посад нет, все попадают в группу «Керівництво коледжу», директор не выделяется
  - Мобильная версия выведена из десктопной: одна колонка, на 375px горизонтального скролла нет
- [x] **Документы (категории)** — `/dokumenty` — `documents/index.blade.php` (светлая шапка-карточка с контуром здания и тремя мини-картами статистики: «N категорій», «N документів у відкритому доступі», крупнейший раздел; живой клиентский поиск по названиям разделов + пилюли-фильтры «Усі розділи / З документами / Порожні» на Alpine, состояние «нічого не знайдено» с кнопкой сброса; сетка карточек 2→3→4 в ряд: иконка папки, бейдж PDF, название, счётчик документов и «Перейти» — пустые категории приглушены и подписаны «Незабаром»; финальный блок «Не знайшли документ?» с DB-контактами из `settings` (скрыт, если все ключи пусты) и CTA на `/kontakty` + `/faq`; тесты — `DocumentsPageTest`)
- [x] **Документы категории** — `/dokumenty/{slug}` — `documents/category.blade.php` (светлая шапка-карточка: бейдж «Публічна інформація», название категории, поле поиска по документам справа, чипы «N документів · Формат PDF · Сторінка X з Y»; **серверный поиск `?q=`** — фильтрация в PHP через `mb_stripos`, потому что `LIKE` в SQLite не видит регистра кириллицы (Gotcha 21), запрос сохраняется в ссылках пагинации; **пагинация 20/стр.** — контроллер переведён с `get()` на `LengthAwarePaginator`; строка «Показано 1–20 із 95 документів» + «Скинути пошук»; строки документов: красная PDF-иконка, кликабельное название (открывает файл в новой вкладке), мета «дата · PDF · размер файла» — размер считает акцессор `Document::file_size_label` по файлу на диске, расширение — `Document::file_extension`; кнопки «Переглянути» (от `sm`) и «Завантажити» (`download` для локальных файлов); сайдбар со всеми разделами и счётчиками (`aria-current` на текущем) — **на мобильном уходит под список документов** (`order-1`/`order-2`), иначе два десятка ссылок отодвигают сами документы; пустые состояния для поиска и для категории без файлов; финальный блок «Не знайшли потрібний документ?»; тесты — `DocumentsPageTest`)
  - Обложек и описаний категорий в БД нет; размера файла тоже — он берётся с диска (`Storage::disk('public')`), для внешних ссылок и отсутствующих файлов не показывается
  - Все 185 импортированных документов — PDF; сортировка из макета ChatGPT (dropdown «Спочатку новіші», выбор «20 на сторінці») не реализована: порядок задаёт админ через `sort_order`

## 4. Динамические страницы (один шаблон `pages/show.blade.php`, 3 варианта отображения)

Шаблон один, но прогнать конвейер на каждом варианте — вёрстка разная:

Шаблон разложен на `pages/show.blade.php` (общая светлая шапка-карточка + общая финальная navy-полоса «Не знайшли потрібну інформацію?») и партиалы в `pages/partials/`: `hub`, `content`, `neighbours`, `toc-list`. Тесты — `PageVariantsTest`.

- [x] **Хаб с дочерними страницами** — `/abituriyentu` (7 хардкод-ссылок — слаг не менять!), `/studentu`, `/pro-koledzh` — `pages/partials/hub.blade.php` (шапка с чипами «N сторінок · N ключових дій · Швидкий пошук у розділі»; блок «Ключові дії» — дочерние страницы с новым флагом `is_featured` («Ключова сторінка розділу», тумблер в Filament; миграция `2026_08_28_170000` помечает 4 стартовые страницы «Абітурієнту»), карточки с чередующимися gold/navy иконками; «Усі сторінки розділу» — пронумерованные карточки 2→3 в ряд с живым поиском на Alpine (появляется от 6 страниц), счётчиком «Знайдено N» и состоянием «нічого не знайдено»; липкий сайдбар «Про розділ» (body страницы) + «Не знайшли потрібну сторінку?» с DB-контактами и CTA)
  - Описаний и иконок у дочерних страниц в БД нет — карточки держатся на номере и названии; описание в «Ключових діях» выводится, только если у страницы заполнен `excerpt`
- [x] **Обычная контентная страница** (prose) — `/biblioteka`, `/sportyvne-zhyttya`, `/litsenzuvannya-ta-akredytatsiya` — `pages/partials/content.blade.php` (шапка: бейдж родительского раздела, чипы «~N хв читання · N файлів · N сторінок у розділі»; двухколоночный `lg:grid-cols-3` — текст на 2 колонки, липкий сайдбар на 1: «Навігація по сторінці» (якоря из h2–h4 — `Page::headings()` / `bodyWithAnchors()`, показывается от 3 заголовков; на мобильном дублируется свёрнутым `<details>` НАД текстом, потому что сайдбар уезжает под 20 000px статьи), «Сусідні сторінки розділу», «Потрібна допомога?» с DB-контактами, кнопка «Нагору»; под текстом — переходы «попередня/наступна сторінка» по порядку из админки)
  - CSS в `app.css`: золотая полоса у заголовков + `scroll-margin-top: 6rem` под липкое меню; ссылки на файлы (pdf/doc/xls/ppt/zip) получают красную иконку документа, а `li` из одной такой ссылки — карточку-строку (селектор `:has()`); таблицы из импорта — navy-шапка, зебра, горизонтальная прокрутка
- [x] **Heritage-страница с drop cap** — `/istoriya` (слаг не менять) — navy-шапка с серифным заголовком и «лист» `heritage-frame` оставлены как есть (осознанно: сайдбар разрушил бы стиль), добавлены блок «Інші сторінки розділу» и общая финальная полоса; drop cap по-прежнему по слагу `istoriya`

## 5. Сервисные экраны

- [x] **Поиск (результаты + пустой запрос + ничего не найдено + подсказки-дропдаун)** — `/poshuk` — `search/index.blade.php` (светлая шапка-карточка вместо navy `page-hero`: бейдж «Пошук по сайту», h1 «Результати за запитом «…»» / «Що вас цікавить?», крупное поле поиска на всю ширину; **чипы-фильтры по типам** `?type=news|pages|specialties|documents|events` со счётчиками — считаются до фильтрации, показываются только непустые; **пагинация 12/стр.** через `LengthAwarePaginator` поверх собранной коллекции, строка «Показано 1–12 із N результатів» + «Показати всі типи»; карточки результата: иконка типа, надпись типа + мета (дата новости / раздел документа / родительская страница / код специальности), подсветка совпадения `<mark>` в названии, уривок 180 символов; внешние ссылки на файлы документов открываются в новой вкладке с иконкой; три пустых состояния — пустой запрос (перечень того, что ищется + «Популярні розділи» из DB-меню), «менше двох символів», «нічого не знайдено» (кнопки в документы/новости/контакты + разделы меню); финальная полоса «Не знайшли потрібну інформацію?» с DB-контактами; тесты — `SearchPageTest`)
  - **Поиск переведён с `LIKE` на PHP-фильтр `mb_stripos`** (Gotcha 21): теперь `?q=положення` находит «Положення …» и на SQLite; тем же путём пошли подсказки в шапке — дропдаун и страница выдают одинаковые результаты
  - В общий поиск добавлены **события** (раньше были только в подсказках); ранжирование простое: сначала совпадения в начале названия, внутри группы — порядок скоупа модели (новости — по дате)
  - Мобильный: проверено на 375px — горизонтального скролла нет, поле 295×52, чипы и кнопки ≥44px (отдельный макет не нужен)
- [x] **Контакты (карта, форма обратной связи + состояния ошибок/успеха)** — `/kontakty` — `contacts.blade.php` (светлая шапка-карточка вместо navy `page-hero`: бейдж «Контакти», h1 «Звʼяжіться з коледжем», лид и три быстрых действия — `tel:` кнопкой, якорь «Написати звернення» → `#forma`, «Прокласти маршрут» на Google Maps по адресу из настроек; ряд из 4 карточек контактов (адреса/телефон/пошта/графік) — только заполненные ключи `settings`, адрес и телефон кликабельны; форма в layout `lg:grid-cols-3` (форма на 2 колонки, липкий сайдбар на 1) — шапка карточки, сводный баннер ошибок «форму не надіслано», зелёный баннер успеха, крупные поля `px-4 py-3 text-base` с `inputmode` tel/email и `autocomplete`, счётчик 0/5000, кнопка со спиннером; сайдбар: «Що буде далі» (3 шага), «Ми в соцмережах» из `social_*`, золотая карточка «Плануєте вступати?» → заявка/FAQ; карта отдельным блоком с шапкой «Як нас знайти», адресом и ссылкой «Відкрити в Google Maps», iframe получил `title`; тесты — `ContactsPageTest`)
  - Все блоки скрываются при пустых настройках (карта, соцсети, маршрут, карточки) — форма остаётся всегда
  - Мобильный: проверено на 375px — горизонтального скролла нет (отдельный макет не нужен)
- [x] **404** — `errors/404.blade.php` (светлая шапка-карточка вместо центрированного блока: бейдж «Помилка 404», h1, `accent-rule`, гигантский полупрозрачный «404» справа на `lg`; живой поиск сохранён (Alpine `liveSearch` + дропдаун подсказок), поле в стиле поиска сайта с кнопкой «Знайти»; кнопки «На головну / Новини / Розклад дзвінків / Контакти» — контракт `ArchiveAndPolishTest` («Що ви шукали?», «Розклад дзвінків») не тронут; ниже — блок «Популярні розділи» карточками из DB-меню (`MenuItem::navigation()`, до 8 пунктов); тесты — `ErrorPagesTest`)
- [x] **403 / 500 / 503** — `errors/{403,500,503}.blade.php` (403 — та же светлая шапка-карточка, что и 404, с бейджем «Помилка 403» и кнопками «На головну / Написати нам / Пошук по сайту»; **500 и 503 намеренно остались автономными** — без layout, компонентов, маршрутов и собранного бандла: они должны отрисоваться, когда приложение сломано или в `artisan down`; переведены с navy-фона на светлую карточку в тех же фирменных цветах (`#16223f`/`#d98e1e` инлайном), бейдж + `rule` + текст, на 500 — кнопка «На головну»; тест `ErrorPagesTest` сторожит автономность: в HTML нет `build/assets` и `container-site`)

## 6. Вне охвата дизайна (не экраны — пропускаем)

- RSS `/novyny/feed.xml`, `/sitemap.xml`, `/robots.txt`, ICS событий — машинные форматы.
- Email-шаблоны `emails/applicant.blade.php`, `emails/feedback.blade.php` — при желании отдельной задачей в конце (инлайн-стили, не Tailwind).
- Filament-админка (`filament/**`) — стандартная тема, редизайн не планируется.

## Чек на каждую задачу

1. Прочитать Feature-тесты на контракты шаблона (grep по классам/aria в `tests/Feature/`).
2. Скрин текущей страницы (десктоп 1920 + мобильный) → ChatGPT-макет.
3. Имплементация: только DB-driven контент, safelist для динамических классов, light-only.
4. `npm run build` → `composer test` (эталон — 230 passed на 2026-08-28) → визуальная проверка 1920px и mobile.
5. Отметить пункт здесь; новые PoC-элементы — в таблицу ARCHITECTURE.md.

## 7. Мобильные версии (после основного прохода)

Правило конвейера: **десктоп-макет — всегда; отдельный мобильный макет — вторым запросом в том же чате ChatGPT** («тепер мобільна версія цієї ж сторінки, 390px» — контекст уже там, почти бесплатно). Для рядовых страниц мобильная версия выводится из десктопной механически (mobile-first Tailwind) и просто проверяется в mobile-viewport — отдельная картинка не нужна.

Экраны, которым нужен ВЫДЕЛЕННЫЙ мобильный макет (структурно другой экран, не «сжатый десктоп»):

- [x] **Хедер/навигация на мобильном** — `components/layouts/app.blade.php`: ряд бренду ниже на телефоне (`h-16`, лого `h-10`), название колледжа не ломает верстку (`min-w-0` + `truncate`); **CTA «Вступнику» теперь виден и на телефоне** (компактный `h-11 px-3 text-xs`) — ради него на `<sm` убрана отдельная иконка поиска (поиск — первым полем в шухляде, иконка возвращается с `sm`); тач-мишени бургера/поиска/крестика 44×44; бургер получил `aria-controls="mobile-menu"` + `:aria-expanded`, меню закрывается по Escape, фон под открытым меню не скролится (`x-effect` вешает `overflow-hidden` на `body`); в шухляде — логотип в шапке, ряд из трёх быстрых действий (Спеціальності / Дзвінки / Контакти), пункты меню по `py-3.5`, а внизу подвал с телефоном, почтой, соцсетями и «Адмінпанель» (утилитарная стрічка `hidden lg:block` на телефоне не показывалась вообще); тесты — `MobileHeaderTest`
  - Проверено на 375px с прод-названием «ВСП ОТФК ОНТУ»: горизонтального скролла нет, шапка не переполняется
  - **Правка после проверки на реальном телефоне (360px):** название обрезалось многоточием — лого `h-9`, название `text-sm`, отступы `gap-2`/`gap-1.5`, кнопка `px-2.5`; теперь на 360px запас ≈22px до обрезки (при `truncate` как страховке для ≤320px)
- [x] **Главная (hero) на мобильном** — `components/home/hero-slider.blade.php`: слайды переехали во внутреннюю «сцену» (`relative` + min-h), сама секция больше не фиксирует высоту, поэтому под сценой появилась **отдельная стрічка фактів на телефоне** (`sm:hidden`, те же топ-3 показателя, что и чипы на десктопе, иконка + значение + подпись, разделители); чипи внутри слайда остаются от `sm`; заголовок на телефоне `text-[2rem]`, кнопки на всю ширину, крапки-переключатели получили тач-мишень 44×32 (сама точка осталась 10px); контенту слайда добавлен нижний отступ (`pb-24 sm:pb-36 lg:pb-32`), иначе на `sm`–`md` крапки/стрелки ложились на чипы
  - **Правка после проверки на реальном телефоне:** слайдер не листался с телефона. Причины и лечение: (1) пауза автопрокрутки висела на `mouseenter`, а тап на мобильном синтезирует mouse-события и глушил таймер навсегда → теперь `@pointerenter/@pointerleave` с проверкой `pointerType === 'mouse'`; (2) не было жестов → добавлен свайп (`@touchstart/@touchend.passive`, порог 40px, после свайпа таймер перезапускается); (3) стрелки `‹ ›` больше не прячутся на `<sm` (мишень 44×44) — иначе неочевидно, что баннер листается. `prefers-reduced-motion` по-прежнему выключает автопрокрутку (у пользователя мог быть включён режим энергосбережения) — ручное листание при этом работает
- [x] **Расписание звонков** — сделано вместе с десктопом: таблица заменена лентой карточек (номер-квадрат + название пары + время справа), на 375px горизонтального скролла нет, строки ≈64px
- [x] **Документы категории** — сделано вместе с десктопом: строка списка перестроена (иконка слева от названия, мета в одну строку, кнопка «Завантажити» под названием, «Переглянути» скрыт — файл открывает сама назва), сайдбар разделов уехал под список документов; на 375px горизонтального скролла нет
- [x] **Заявка абитуриента `/zayavka`** — сделано вместе с десктопом (мобильный макет — вторым промптом в том же чате): одна колонка, крупные поля под палец, `inputmode` tel/email, проверено на 375px — горизонтального скролла нет, состояния ошибок и успеха отрисованы
- [x] **Квиз** — сделано вместе с десктопом: одна колонка, варианты 295×80, CTA 295×52, прогресс-сегменты на всю ширину; на 375px горизонтального скролла нет ни на одном из трёх экранов

Проверка каждого пункта: preview `otfk-alt` в mobile-viewport (375px) + реальные данные из прод-копии БД; следить за тач-мишенями ≥44px и отсутствием горизонтального скролла.
